export const getSubstudyTextColor = (color: string) => {
  return `text-study-${color}`;
}

export const getSubstudyBgColor = (color: string) => {
  return `bg-study-${color}`;
}

export const getSubstudyBorderColor = (color: string) => {
  return `border-study-${color}`;
}
